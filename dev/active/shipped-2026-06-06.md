# Shipped 2026-06-06 — testing reference

One-page guide to everything shipped today, organized by where to test.

---

## `/admin/place` — operator real-money decisions

| Change | What to check |
|---|---|
| **Real-Money Tier badge per bet** (✅ Bet / 🟡 Cautious / 🔬 Paper only) | Each bet row should show ONE small chip. Hover reveals model + bot + per-bet flag breakdown. Bets are NEVER filtered — all pending paper bets still surface |
| **Coolbet ingest banner** (amber, top of page) | Should appear ONLY when no Coolbet `odds_snapshots` arrived in the last 60min. Today, expect it NOT to show (cron healthy) |

Commits: `web@31d3891`, `engine@95c4c1d`

---

## `/value-bets` — VALUE-BETS-DENSITY-PASS

| Change | What to check |
|---|---|
| **Compact page header** (was 5 stacked sections, now 2 lines) | Above the fold on mobile should show: title + CLV pill + Telegram icon + 1-line caption. ~500-600px of vertical recovered. |
| **CLV stat is a clickable pill** linking to `/performance` | Click `+10.0% CLV · 30d` should land on `/performance` |
| **Telegram CTA is icon-chip** (not full-width banner) | Click 📲 icon should land on `/profile#telegram` |
| **Tier explainer collapsed** to one-line caption | No more 3-line "You're seeing the calibrated/full feed" blocks |
| **Live picks: compact 1-row strip** (was full grid) | Shows top 3 live picks + "→ /live" link. Click `→ /live` for full grid. |
| **Per-row "Calibrated" chip** (Elite only, was "Pro") | Orange chip on Elite-view picks marked as calibrated cohort |
| **`TodayPicksPreview` table removed for Pro/Elite** | Free users still see it; Pro/Elite no longer see the duplicate-data table |

Commit: `web@091bed8`

---

## `/live` — unchanged but referenced

The full live grid still lives here. `/value-bets` strip links to it.

---

## `/admin/real-bets` — void count fix

| Change | What to check |
|---|---|
| **"Won / lost / void" tile** | When voids exist today, label changes to include them. Today: 5 won / 7 lost / 1 void = 13 settled (matches the math). |
| **Hit-rate denominator excludes voids** | `won / (won+lost)` not `won/settled` — voids don't dilute the signal |

Commit: `web@58a00fd`

---

## Engine — model + calibration improvements (silent to user, affects bot picks)

| Change | What it does |
|---|---|
| **6 fresh Platt calibration rows landed at 10:35 UTC** (`asian_handicap_away -0.5`, `btts_yes/no`, `double_chance_1x/x2`, `inplay_e_under_25`) | Bot probability outputs are now better calibrated on these markets. Effect surfaces over next 24-48h |
| **inplay_i Bayesian Gamma update + model-vs-market gate** | Favourite Stall strategy now applies posterior xG conditioning + skips low-data-league overconfidence. Fewer bets but better expected EV |
| **inplay_p investigation closed** | Retired bot's data confirmed retirement was correct. v2 active, gate-filed at 100 bets |
| **inplay_n model-vs-market gate** | Skips picks where prematch model claims ≥62% but market priced same side <47% (15pp gap) |
| **inplay_o quarantine** | 62 pre-fix tainted bets voided; `bot_inplay_o` bankroll corrected from €1591.95 → €1214.44 |
| **`bot_ah_away_dog` retired** | CLV-BACKFILL evidence: −29.87% CLV / n=8. Daily pipeline now skips |
| **`bot_ah_home_fav` promoted to calibrated** | +8.89% CLV / n=14 — eligible for real-money cohort when CHERRY-PICK-PLACER Phase 3 flips |
| **`bot_aggressive_v2` retired** | Persistent loser: −21.95% ROI / +6.46% CLV — sized into bad odds |
| **5 acca/combo bots retired** | 0W/42L combined — structural-EV failure |

Engine commits: `9ccf980`, `e6ff7c1`, `bec4d14`, `3ec0bb3`, `48daaf0`, `081fd037`, `f9354be`, `36fc589`, `4bf6194`

---

## Engine — automation (operator-relevant)

| Change | When it fires |
|---|---|
| **Weekly threshold check cron** | Sun 06:00 UTC — emails `scripts/threshold_check.py` snapshot of all gate counts |
| **WC odds daily sweep** | Daily 06:30 UTC, gated to 2026-06-11..07-19 window. Requires `OA_KEY` on Railway ✓ (you added today) |
| **WC daily preview email** | Daily 07:30 UTC, gated to WC window |

Engine commits: `9ccf980`, `d381474`, `604f928`

---

## Railway env vars set today

| Var | Status | Purpose |
|---|---|---|
| `OA_KEY` | ✓ Added | The Odds API authentication (WC odds sweep) |
| `GATE_EVENTS_BY_COVERAGE=true` | ✓ Added | Skip /fixtures/events AF calls for leagues with `coverage_events=false` |
| `BOT_COHORT_OVERRIDES=bot_opt_away_british:morning,bot_opt_away_europe:morning` | ✓ Added | Route away-1X2 bots to 07:00 UTC peak (ODDS-TIMING-OPT) |
| `SHADOW_MODEL_VERSION=v14_recreate_2026_05_11` | ✓ Added | 12-24h shadow comparison for tomorrow's OU pin decision |

Each save triggered a Railway redeploy + scheduler restart. The "killed — scheduler restarted" failures in `pipeline_runs` between 09:00-14:50 UTC are graceful kills from those redeploys, not a hang. Post-15:00 UTC clean.

---

## Sunday 6/7 expectations (TOMORROW)

| Time UTC | Auto-fires | What to watch |
|---|---|---|
| 02:30 | `league_clv_efficiency` | — |
| 03:00 | `weekly_retrain` → produces `v20260607` | Watch logs for which markets fit/skip |
| 04:00 | `weekly_meta_retrain` | New B-ML3 bundle (likely fail 5pp gate per today's preview) |
| 05:00 | `weekly_meta_validate` (emails verdict) | Should land in inbox |
| 06:00 | `weekly_threshold_check` (emails snapshot) | First-ever automated run — should arrive |
| 06:30 | `wc_odds_sweep` | Will SKIP (gated to 6/11+) |
| 07:30 | `wc_match_previews` + `wc_daily_email` | Both will SKIP (gated to 6/11+, but cron registered) |

**Sunday morning operator decisions:**
1. Read `shadow_bets` for OU CLV under `v14_recreate_2026_05_11` cohort
2. 3-way model compare with `v20260607`
3. Phase 4 verdict re-run (`scripts/real_perf_split_by_source.py --days 14`)
4. Per-market env flips (`MODEL_VERSION_1X2`, possibly `MODEL_VERSION_OU` + `MODEL_VERSION_GOALS`)
5. CHERRY-PICK-PLACER Phase 3 flip (`COOLBET_RECORD_ALLOWED_MATURITY=calibrated`) — only if Phase 4 positive
6. Re-run `scripts/check_post_calibration_impact.py` for meaningful 24h comparison

---

## Total session today

**21 commits across both repos.** No production deploys broke (the "failures" in pipeline_runs are env-var-restart side effects, not bugs).

**No outstanding issues** — Saturday work complete. Sunday cron + your testing pick up from here.
