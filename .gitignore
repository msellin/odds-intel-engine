venv/
__pycache__/
*.pyc
.env
# .env backups (e.g. .env.pre-cutover-*, .env.bak-*) — contain live DB
# credentials, must never be committed.
.env.*
data/raw/
data/processed/
data/models/
data/model_results/
data/daily/
data/logs/
data/esports/
*.pkl
*.joblib

# Model artifacts needed on Railway — force-tracked with `git add -f` (gitignore
# negation doesn't work inside ignored directories; once tracked, git keeps them)
# Files: data/processed/{features_v9,targets_poisson_history,targets_global,targets_extended}.csv
#        data/models/soccer/v9a_202425/*.pkl
.DS_Store
/.idea/
supabase/.temp/
.claude/

# Large vendor docs (keep locally, not in repo)
docs/*.pdf

# Generated reports
sonarcloud_report.*
lighthouse-*.json
lighthouse-*.html


# Backfill replay outputs (regenerated by scripts/replay_inplay.py)
dev/active/inplay-backfill-*.csv
dev/active/inplay-backfill-*.txt
# Pinnacle historical backfill raw JSON dumps (~150 MB, regenerable via OddsPapi)
dev/active/pinnacle-backfill-jsons/
.claude/settings.local.json
.coolbet_session/

# Migration secrets (temporary during Supabase→VPS migration)
dev/active/supabase-migration-secrets.md

# CS2-REMOVAL-2026-08-26: 41MB table dump, kept locally + on VPS /tmp, never committed
dev/active/cs2-removal-2026-08-26/*.gz

# Working artefacts under dev/active — logs, raw captures and bulk exports.
# Added 2026-08-31 after a `git add -A` swept 69 of these into a commit,
# including a 63 MB coolbet-odds-snapshot.log that tripped GitHub's size
# warning. Plans/context/tasks markdown stays tracked; the noise does not.
dev/active/*.log
dev/active/**/*.log
dev/active/*.csv
dev/active/**/*.csv
dev/active/*.pdf
dev/active/coolbet-raw-*.json
dev/active/deepbetting_stats.json
