venv/
__pycache__/
*.pyc
.env
data/raw/
data/processed/
data/models/
data/model_results/
data/daily/
data/logs/
*.pkl
*.joblib

# Model artifacts needed on Railway — force-tracked with `git add -f` (gitignore
# negation doesn't work inside ignored directories; once tracked, git keeps them)
# Files: data/processed/{features_v9,targets_v9,targets_global}.csv
#        data/models/soccer/v9a_202425/*.pkl
.DS_Store
/.idea/
supabase/.temp/
.claude/

# Large vendor docs (keep locally, not in repo)
docs/*.pdf

# Generated reports
sonarcloud_report.*
lighthouse-*.json
lighthouse-*.html


# Backfill replay outputs (regenerated by scripts/replay_inplay.py)
dev/active/inplay-backfill-*.csv
dev/active/inplay-backfill-*.txt
