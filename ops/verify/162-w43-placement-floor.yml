# #162 W4.3 — one placement floor (placement_floor.pick_clears), 2026-09-25.
task: "#162"
checks:
  - name: every_capable_bot_exports_its_placement_floor
    mode: eventually
    # export_bot_config (03:40 UTC) writes the real-money rule per selection as the 'placement_floor' gate.
    sql: |
      SELECT count(*) FROM bot_config
       WHERE placeable
         AND NOT EXISTS (SELECT 1 FROM jsonb_array_elements(gates) g WHERE g->>'name' = 'placement_floor')
    expect: "== 0"
    run_after: {job: export_bot_config, since: "2026-09-25T23:00:00Z"}
    expires: "2026-10-03T00:00:00Z"
