# #168a — the queue itself: migration 447 applied and the job has run on the VPS.
task: "#168"
checks:
  - name: verify_queue_job_ran
    mode: eventually
    sql: SELECT count(*) FROM pipeline_runs WHERE job_name = 'verify_queue' AND status = 'completed'
    expect: ">= 1"
    run_after: {migration: "447"}
    expires: "2026-09-26T18:00:00Z"
