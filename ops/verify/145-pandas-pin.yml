# [[#145]] pandas 3.0.4 segfaults on tz-aware datetime take/groupby/merge on the VPS (reproduced
# 2026-09-26: exit 139 on 3.0.4; 3.0.2 / 3.0.3 / 3.0.5 / 3.0.6 clean). requirements.txt now says !=3.0.4.
task: "#145"
checks:
  - name: vps_pandas_not_304
    mode: eventually
    cmd: ["./venv/bin/python", "-c", "import pandas; print(pandas.__version__ != '3.0.4')"]
    expect: "true"
    expires: "2026-09-28T00:00:00Z"
  - name: tz_datetime_repro_runs_clean
    mode: eventually
    cmd: ["./venv/bin/python", "-c", "import pandas as pd, numpy as np; df = pd.DataFrame({'t': pd.date_range('2026-01-01', periods=20000, freq='min', tz='UTC'), 'g': np.arange(20000) % 7}); df.groupby('g').t.max(); df.take(np.arange(0, 20000, 3)); df[df.g > 2].merge(df, on='g'); print('true')"]
    expect: "true"
    expires: "2026-09-28T00:00:00Z"
